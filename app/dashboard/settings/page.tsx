'use client';

import { useEffect, useState } from 'react';
import Link from 'next/link';
import { PageHeader } from '@/components/dashboard/PageHeader';
import { Surface } from '@/components/dashboard/Surface';
import { StatusBadge } from '@/components/dashboard/StatusBadge';
import { ProgressBar } from '@/components/dashboard/ProgressBar';
import { FormField } from '@/components/dashboard/FormField';

const VISIT_LIMITS: Record<string, number> = { starter: 500, growth: 5000, pro: Infinity };
const PLAN_LABELS: Record<string, string> = { starter: 'Starter', growth: 'Growth', pro: 'Pro' };
const DOMAIN_LIMITS: Record<string, number> = { starter: 1, growth: 3, pro: 10 };
type ClientDomain = { id: string; origin: string | null; domain: string; is_primary: boolean; active: boolean };

export default function SettingsPage() {
  const [profileState, setProfileState] = useState<'loading' | 'ready' | 'error'>('loading');
  const [domainState, setDomainState] = useState<'loading' | 'ready' | 'error'>('loading');
  const [domain, setDomain] = useState('');
  const [persistedDomain, setPersistedDomain] = useState('');
  const [companyName, setCompanyName] = useState('');
  const [plan, setPlan] = useState<string | null>(null);
  const [monthlyVisits, setMonthlyVisits] = useState(0);
  const [domains, setDomains] = useState<ClientDomain[]>([]);
  const [newDomain, setNewDomain] = useState('');
  const [mutation, setMutation] = useState<'idle' | 'saving' | 'adding' | 'removing'>('idle');
  const [removingId, setRemovingId] = useState<string | null>(null);
  const [message, setMessage] = useState<{ type: 'success' | 'error'; text: string } | null>(null);

  const loadProfile = async () => { setProfileState('loading'); try { const res = await fetch('/api/client'); if (!res.ok) throw new Error(); const data = await res.json(); if (!data.client) throw new Error(); const nextDomain = data.client.domain || ''; setDomain(nextDomain); setPersistedDomain(nextDomain); setCompanyName(data.client.company_name || ''); setPlan(data.client.plan || null); setMonthlyVisits(data.client.monthly_visits || 0); setProfileState('ready'); } catch { setProfileState('error'); } };
  const loadDomains = async () => { setDomainState('loading'); try { const res = await fetch('/api/client/domains'); if (!res.ok) throw new Error(); const data = await res.json(); setDomains(data.domains || []); setDomainState('ready'); } catch { setDomainState('error'); } };
  useEffect(() => { void loadProfile(); void loadDomains(); }, []);

  const handleSave = async () => { setMutation('saving'); setMessage(null); try { const res = await fetch('/api/client', { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ domain }) }); const data = await res.json(); if (!res.ok || !data.success) throw new Error(data.error || 'Failed to update domain.'); const next = data.domain || domain; setDomain(next); setPersistedDomain(next); setMessage({ type: 'success', text: 'Primary domain updated successfully.' }); } catch (error) { setDomain(persistedDomain); setMessage({ type: 'error', text: error instanceof Error ? error.message : 'Network error occurred.' }); } finally { setMutation('idle'); } };
  const handleAddDomain = async () => { if (!newDomain.trim()) return; setMutation('adding'); setMessage(null); try { const res = await fetch('/api/client/domains', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ domain: newDomain }) }); const data = await res.json(); if (!res.ok) throw new Error(data.error || 'Failed to add domain'); setNewDomain(''); await loadDomains(); setMessage({ type: 'success', text: 'Domain added.' }); } catch (error) { setMessage({ type: 'error', text: error instanceof Error ? error.message : 'Failed to add domain.' }); } finally { setMutation('idle'); } };
  const handleRemoveDomain = async (id: string) => { setMutation('removing'); setRemovingId(id); setMessage(null); try { const res = await fetch(`/api/client/domains?id=${encodeURIComponent(id)}`, { method: 'DELETE' }); const data = await res.json(); if (!res.ok) throw new Error(data.error || 'Failed to remove domain'); await loadDomains(); setMessage({ type: 'success', text: 'Domain removed.' }); } catch (error) { setMessage({ type: 'error', text: error instanceof Error ? error.message : 'Failed to remove domain.' }); } finally { setMutation('idle'); setRemovingId(null); } };

  const activeDomains = domains.filter((item) => item.active).length;
  const visitLimit = plan ? VISIT_LIMITS[plan] : null;
  const visitPct = visitLimit && visitLimit !== Infinity ? Math.min((monthlyVisits / visitLimit) * 100, 100) : 0;
  const tone: 'danger' | 'warning' | 'accent' = visitPct >= 90 ? 'danger' : visitPct >= 70 ? 'warning' : 'accent';
  const busy = mutation !== 'idle';

  return <div className="dashboard-settings max-w-5xl space-y-8"><PageHeader eyebrow="Signal Field · Workspace controls" title="Settings" description="Manage your workspace, domains, and usage." />
    {message && <p role={message.type === 'success' ? 'status' : 'alert'} aria-live="polite" className={message.type === 'success' ? 'text-sm text-[var(--green)]' : 'text-sm text-[var(--red)]'}>{message.text}</p>}
    <div className="grid grid-cols-1 gap-6 lg:grid-cols-2">
      <Surface className="space-y-6"><div><p className="dashboard-eyebrow">Workspace identity</p><h2 className="mt-2 text-lg font-semibold text-[var(--text-primary)]">Your workspace</h2><p className="mt-1 text-sm text-[var(--text-secondary)]">Choose where Churnaut should listen for signals.</p></div>{profileState === 'loading' ? <div role="status" aria-busy="true" aria-label="Loading workspace profile" className="space-y-3 motion-safe:animate-pulse"><div className="h-10 rounded-xl bg-[var(--border-subtle)]" /><div className="h-10 rounded-xl bg-[var(--border-subtle)]" /></div> : profileState === 'error' ? <div role="alert" className="rounded-xl border border-[var(--red)]/30 bg-[var(--red)]/5 p-4 text-sm text-[var(--red)]">Workspace profile unavailable. <button type="button" onClick={loadProfile} className="font-semibold underline">TRY AGAIN</button></div> : <div className="space-y-5"><div><p className="dashboard-field-label">Company name</p><div className="mt-1 rounded-xl border border-[var(--border-subtle)] bg-[var(--bg-base)] px-3 py-2.5 text-sm text-[var(--text-muted)]" aria-readonly="true">{companyName || '—'}</div></div><fieldset className="space-y-2"><legend className="dashboard-field-label">Registered domains</legend>{domainState === 'loading' ? <div role="status" aria-busy="true" className="text-sm text-[var(--text-muted)]">Loading domains…</div> : domainState === 'error' ? <p role="alert" className="text-sm text-[var(--red)]">Domain registry unavailable. <button type="button" onClick={loadDomains} className="font-semibold underline">TRY AGAIN</button></p> : domains.length === 0 ? <p className="text-sm text-[var(--text-muted)]">No domains registered yet.</p> : domains.map((item) => { const value = item.origin || item.domain; return <div key={item.id} className="flex items-center gap-3 rounded-xl border border-[var(--border-subtle)] p-3"><input type="radio" id={`primary-${item.id}`} name="primary-domain" checked={domain === value} onChange={() => setDomain(value)} aria-label={`Make ${value} primary`} /><label htmlFor={`primary-${item.id}`} className="min-w-0 flex-1 truncate text-sm text-[var(--text-primary)]">{value}</label>{activeDomains > 1 && <button type="button" disabled={busy} onClick={() => void handleRemoveDomain(item.id)} className="text-xs text-[var(--red)] transition hover:underline disabled:opacity-50">{mutation === 'removing' && removingId === item.id ? 'REMOVING…' : 'Remove'}</button>}</div>; })}<p className="text-xs text-[var(--text-muted)]">{activeDomains} / {plan ? (DOMAIN_LIMITS[plan] || 1) : '—'} domains used</p></fieldset><div className="flex items-end gap-2"><FormField id="new-domain" label="Add a domain"><input id="new-domain" type="url" value={newDomain} onChange={(event) => setNewDomain(event.target.value)} placeholder="https://yourwebsite.com" className="w-full rounded-xl border border-[var(--border-subtle)] bg-[var(--bg-base)] px-3 py-2.5 text-sm text-[var(--text-primary)] outline-none focus:border-[var(--accent)]" /></FormField><button type="button" disabled={busy || !newDomain.trim() || !plan || activeDomains >= (DOMAIN_LIMITS[plan] || 1)} onClick={() => void handleAddDomain()} className="mb-0.5 dashboard-button-secondary disabled:opacity-40">{mutation === 'adding' ? 'ADDING…' : 'Add'}</button></div><button type="button" onClick={() => void handleSave()} disabled={busy || domain === persistedDomain} className="w-full dashboard-button-primary disabled:opacity-50">{mutation === 'saving' ? 'SAVING…' : 'Save Primary Domain'}</button></div>}</Surface>
      <Surface className="space-y-6"><div><p className="dashboard-eyebrow">Capacity</p><h2 className="mt-2 text-lg font-semibold text-[var(--text-primary)]">Your capacity</h2><p className="mt-1 text-sm text-[var(--text-secondary)]">Monitor this month&apos;s tracked visits and plan limits.</p></div>{profileState === 'loading' ? <div role="status" aria-busy="true" aria-label="Loading workspace capacity" className="space-y-3 motion-safe:animate-pulse"><div className="h-8 w-1/3 rounded-xl bg-[var(--border-subtle)]" /><div className="h-3 rounded-full bg-[var(--border-subtle)]" /></div> : profileState === 'error' ? <div role="alert" className="rounded-xl border border-[var(--red)]/30 bg-[var(--red)]/5 p-4 text-sm text-[var(--red)]">Capacity unavailable. <button type="button" onClick={loadProfile} className="font-semibold underline">TRY AGAIN</button></div> : <div className="space-y-6"><div className="flex items-center gap-3"><span className="text-sm text-[var(--text-secondary)]">Current plan</span><StatusBadge tone="info">{PLAN_LABELS[plan || ''] || plan || 'Unknown'}</StatusBadge></div>{visitLimit === Infinity ? <p className="text-sm text-[var(--green)]">Unlimited tracked visits</p> : visitLimit ? <ProgressBar value={visitPct} tone={tone} label={`Tracked visits this month · ${monthlyVisits.toLocaleString()} / ${visitLimit.toLocaleString()}`} /> : <p className="text-sm text-[var(--text-muted)]">Visit capacity unavailable.</p>}<p className="text-xs text-[var(--text-muted)]">Usage resets on the 1st of each month.</p><Link href="/dashboard/billing" className="dashboard-button-secondary block w-full text-center">Manage Billing →</Link></div>}</Surface>
    </div>
  </div>;
}
