'use client';

import { useEffect, useState } from 'react';
import { toast } from '@/hooks/useToast';
import { PageHeader } from '@/components/dashboard/PageHeader';
import { SectionHeader as BaseSectionHeader } from '@/components/dashboard/SectionHeader';
function SectionHeader({ title, description }: { title: string; description?: string; eyebrow?: string }) { return <BaseSectionHeader title={title} description={description} />; }
import { Surface } from '@/components/dashboard/Surface';

interface ClientProfile { webhook_secret: string; webhook_query_auth_expires_at?: string | null; webhook_previous_secret_expires_at?: string | null }
interface WebhookMapping { id: string; external_field: string; internal_field: string }
interface WebhookLog { id: string; created_at: string; session_id?: string; metadata?: { webhook_action?: string; webhook_auth_method?: 'bearer' | 'signature' | 'legacy_query'; payload_key_count?: number; transformed_field_count?: number; result_category?: string } }
const INTERNAL_FIELDS = [
  ['session_id', 'Session ID', 'Identifies the Churnaut link session to update (optional)'], ['prospect_name', 'Prospect Name', 'Full name of the contact'], ['prospect_email', 'Prospect Email', 'Main email address used for contact lookups'], ['company_name', 'Company Name', "Name of the prospect's organization"], ['job_title', 'Job Title', 'Professional title or role'], ['assigned_rep', 'Assigned Rep', 'Representative name for personalized scheduling'], ['calendar_url', 'Calendar URL', 'Meeting link for direct calendar booking embeds'], ['crm_deal_id', 'CRM Deal ID', 'External identifier reference for CRM integration'], ['deal_stage', 'Deal Stage', 'Current pipeline stage for personalized routing'], ['visitor_type', 'Visitor Type', 'Segment tag such as Enterprise or Self-Serve'], ['converted', 'Converted', 'Boolean status indicating converted accounts'],
] as const;

export default function WebhooksSettingsPage() {
  const [client, setClient] = useState<ClientProfile | null>(null); const [mappings, setMappings] = useState<WebhookMapping[]>([]); const [logs, setLogs] = useState<WebhookLog[]>([]); const [loading, setLoading] = useState(true); const [copiedUrl, setCopiedUrl] = useState(false); const [copiedToken, setCopiedToken] = useState(false); const [rotatingSecret, setRotatingSecret] = useState(false); const [sourceFields, setSourceFields] = useState(['email', 'first_name', 'last_name', 'company', 'title', 'deal_id', 'stage', 'rep_name', 'rep_calendar', 'is_converted']); const [newSourceField, setNewSourceField] = useState(''); const [draggedField, setDraggedField] = useState<string | null>(null); const [hoveredTarget, setHoveredTarget] = useState<string | null>(null); const [expandedLogId, setExpandedLogId] = useState<string | null>(null); const [selectedSourceField, setSelectedSourceField] = useState('');
  const fetchData = async () => { try { setLoading(true); const clientRes = await fetch('/api/client'); if (clientRes.ok) setClient((await clientRes.json()).client); const mappingsRes = await fetch('/api/webhook/mappings'); if (mappingsRes.ok) setMappings((await mappingsRes.json()).mappings || []); const logsRes = await fetch('/api/webhook/logs'); if (logsRes.ok) setLogs((await logsRes.json()).logs || []); } catch (error) { console.error('Failed to load webhook settings:', error); } finally { setLoading(false); } };
  useEffect(() => { fetchData(); }, []);
  const webhookUrl = typeof window !== 'undefined' ? `${window.location.origin}/api/webhook` : '/api/webhook';
  const copy = async (value: string, kind: 'url' | 'token') => { await navigator.clipboard.writeText(value); if (kind === 'url') { setCopiedUrl(true); window.setTimeout(() => setCopiedUrl(false), 2000); } else { setCopiedToken(true); window.setTimeout(() => setCopiedToken(false), 2000); } };
  const rotate = async () => { if (!window.confirm('Rotate the webhook secret? Existing URL authentication will stop immediately.')) return; setRotatingSecret(true); try { const res = await fetch('/api/webhook/secret/rotate', { method: 'POST' }); const data = await res.json(); if (!res.ok) throw new Error(data.error || 'Unable to rotate webhook secret'); setClient((prev) => prev ? { ...prev, webhook_secret: data.webhook_secret, webhook_query_auth_expires_at: null, webhook_previous_secret_expires_at: data.previous_secret_expires_at } : prev); toast.success('Webhook secret rotated. Update your sender headers now.'); } catch (error) { toast.error(error instanceof Error ? error.message : 'Unable to rotate webhook secret'); } finally { setRotatingSecret(false); } };
  const addSource = (event: React.FormEvent) => { event.preventDefault(); const value = newSourceField.trim(); if (value && !sourceFields.includes(value)) setSourceFields((prev) => [...prev, value]); setNewSourceField(''); };
  const saveMapping = async (externalField: string, internalField: string) => { try { const res = await fetch('/api/webhook/mappings', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ external_field: externalField, internal_field: internalField }) }); if (!res.ok) throw new Error((await res.json().catch(() => ({}))).error || 'Failed to save webhook mapping'); const data = await res.json().catch(() => ({})); setMappings((prev) => [...prev.filter((m) => m.internal_field !== internalField), data.mapping || { id: crypto.randomUUID(), external_field: externalField, internal_field: internalField }]); toast.success('Webhook mapping saved'); setSelectedSourceField(''); } catch (error) { toast.error(error instanceof Error ? error.message : 'An error occurred while saving the mapping.'); } };
  const removeMapping = async (id: string) => { try { const res = await fetch(`/api/webhook/mappings?id=${id}`, { method: 'DELETE' }); if (!res.ok) throw new Error('Failed to delete mapping.'); setMappings((prev) => prev.filter((mapping) => mapping.id !== id)); toast.success('Webhook mapping deleted successfully'); } catch (error) { toast.error(error instanceof Error ? error.message : 'An error occurred during mapping deletion.'); } };
  const mapped = (key: string) => mappings.find((mapping) => mapping.internal_field === key);
  const drop = async (event: React.DragEvent, key: string) => { event.preventDefault(); setHoveredTarget(null); if (draggedField) await saveMapping(draggedField, key); setDraggedField(null); };

  return <div className="space-y-6"><PageHeader eyebrow="Signal Room · Event intake" title="Webhook settings" description="Configure secure event intake, map external fields, and inspect operational telemetry." />{loading ? <div className="dashboard-surface p-8 text-sm text-[var(--text-muted)]">RETRIEVING WEBHOOK SYSTEM STATUS…</div> : <><Surface><SectionHeader eyebrow="Endpoint & authentication" title="Universal webhook credentials" description="Use the plain endpoint with a bearer token or signed headers. Secrets are never placed in URLs." /><div className="mt-5 grid gap-5 lg:grid-cols-2"><div><label htmlFor="webhook-url" className="dashboard-field-label">Webhook URL</label><div className="mt-2 flex gap-2"><input id="webhook-url" readOnly value={webhookUrl} className="dashboard-input min-w-0 flex-1 font-mono text-xs" /><button type="button" onClick={() => copy(webhookUrl, 'url')} className="dashboard-button-primary shrink-0">{copiedUrl ? 'COPIED!' : 'COPY'}</button></div></div><div><label htmlFor="webhook-token" className="dashboard-field-label">Authorization Bearer Token (Recommended)</label><div className="mt-2 flex gap-2"><input id="webhook-token" readOnly value={client?.webhook_secret || ''} className="dashboard-input min-w-0 flex-1 font-mono text-xs" /><button type="button" onClick={() => copy(client?.webhook_secret || '', 'token')} className="dashboard-button-primary shrink-0">{copiedToken ? 'COPIED!' : 'COPY'}</button></div><p className="mt-2 text-xs text-[var(--text-muted)]">Send <code>Authorization: Bearer &lt;token&gt;</code>. Custom senders may sign exact request bodies with X-Churnaut-Client-Id, X-Churnaut-Timestamp, and X-Churnaut-Signature.</p><button type="button" onClick={rotate} disabled={rotatingSecret} className="dashboard-button-danger mt-4">{rotatingSecret ? 'ROTATING…' : 'ROTATE WEBHOOK SECRET'}</button>{client?.webhook_previous_secret_expires_at && <p className="mt-2 text-xs text-[var(--text-muted)]">Previous header/signature credential expires {new Date(client.webhook_previous_secret_expires_at).toLocaleString()}.</p>}{client?.webhook_query_auth_expires_at && Date.parse(client.webhook_query_auth_expires_at) > Date.now() && <p className="mt-2 text-xs text-[var(--amber)]"><strong>Legacy URL:</strong> older integrations remain supported until {new Date(client.webhook_query_auth_expires_at).toLocaleString()}; new setups must use headers.</p>}</div></div></Surface>
  <Surface><SectionHeader eyebrow="Field mapper" title="Map incoming fields" description="Drag a source field or choose one from the target control, then press MAP. Both paths use the same mapping request." /><div className="mt-5 grid gap-5 lg:grid-cols-[0.8fr_1.2fr]"><section aria-labelledby="external-fields-heading" className="rounded-[var(--radius-md)] border border-[var(--border-subtle)] bg-[var(--bg-elevated)]/45 p-4"><h2 id="external-fields-heading" className="font-mono text-sm font-semibold">External payload fields</h2><p className="mt-2 text-xs text-[var(--text-muted)]">Drag these blocks into a target, or use the keyboard mapping controls.</p><form onSubmit={addSource} className="mt-4 flex gap-2"><label htmlFor="custom-source-field" className="sr-only">Custom source field</label><input id="custom-source-field" value={newSourceField} onChange={(event) => setNewSourceField(event.target.value)} placeholder="e.g. contact.stage_name" className="dashboard-input min-w-0 flex-1 text-xs" /><button type="submit" className="dashboard-button-secondary">ADD</button></form><ul aria-label="External payload fields" className="mt-4 flex flex-wrap gap-2">{sourceFields.map((field) => <li key={field} draggable onDragStart={() => setDraggedField(field)} className="cursor-grab rounded border border-[var(--border-subtle)] bg-[var(--bg-surface)] px-3 py-2 font-mono text-xs text-[var(--text-secondary)]">:: {field}</li>)}</ul></section><section aria-labelledby="internal-fields-heading" className="rounded-[var(--radius-md)] border border-[var(--border-subtle)] bg-[var(--bg-elevated)]/45 p-4"><h2 id="internal-fields-heading" className="font-mono text-sm font-semibold">Churnaut internal fields</h2><p className="mt-2 text-xs text-[var(--text-muted)]">Target fields used by personalization and routing.</p><div className="mt-4 space-y-3">{INTERNAL_FIELDS.map(([key, label, desc]) => { const active = mapped(key); const hovered = hoveredTarget === key; return <div key={key} onDragOver={(event) => { event.preventDefault(); setHoveredTarget(key); }} onDragLeave={() => setHoveredTarget(null)} onDrop={(event) => drop(event, key)} className={`rounded-[var(--radius-md)] border p-3 ${hovered ? 'border-[var(--accent)] bg-[var(--accent)]/5' : 'border-[var(--border-subtle)] bg-[var(--bg-surface)]/45'}`}><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h3 className="text-sm font-semibold text-[var(--text-secondary)]">{label}</h3><p className="mt-1 text-xs text-[var(--text-muted)]">{desc}</p></div>{active ? <div className="flex items-center gap-2"><code className="rounded border border-[var(--border-subtle)] px-2 py-1 text-xs text-[var(--accent)]">{active.external_field}</code><button type="button" aria-label={`Remove mapping for ${label}`} onClick={() => removeMapping(active.id)} className="text-xs text-[var(--red)]">REMOVE</button></div> : <div className="flex items-center gap-2"><label htmlFor={`map-${key}`} className="sr-only">Map {label}</label><select id={`map-${key}`} aria-label={`Map ${label}`} value={selectedSourceField} onChange={(event) => setSelectedSourceField(event.target.value)} className="dashboard-input max-w-[170px] text-xs"><option value="">Select field…</option>{sourceFields.map((source) => <option key={source} value={source}>{source}</option>)}</select><button type="button" aria-label={`Map selected field to ${label}`} onClick={() => saveMapping(selectedSourceField, key)} disabled={!selectedSourceField} className="dashboard-button-secondary disabled:opacity-40">MAP</button></div>}</div></div>; })}</div></section></div></Surface>
  <Surface><SectionHeader eyebrow="Ingestion log" title="Incoming webhook logs" description="Operational metadata only: payload values are intentionally not retained." />{logs.length === 0 ? <div className="mt-5 rounded border border-[var(--border-subtle)] p-8 text-center text-sm text-[var(--text-muted)]">No webhook calls logged yet.</div> : <div role="region" aria-label="Incoming webhook logs" className="mt-5 overflow-hidden rounded border border-[var(--border-subtle)]"><ul>{logs.map((log) => { const expanded = expandedLogId === log.id; const method = log.metadata?.webhook_auth_method; const panel = `webhook-log-${log.id}`; return <li key={log.id} className="border-b border-[var(--border-subtle)] last:border-0"><button id={`${panel}-trigger`} type="button" aria-expanded={expanded} aria-controls={panel} onClick={() => setExpandedLogId(expanded ? null : log.id)} className="flex w-full items-center justify-between gap-4 px-4 py-3 text-left"><span className="flex flex-wrap items-center gap-3"><span className="rounded border border-[var(--green)]/30 px-2 py-1 text-[10px] text-[var(--green)]">POST</span><span className="text-sm text-[var(--text-secondary)]">{log.metadata?.webhook_action || 'processed'}</span>{method && <span className="rounded border border-[var(--border-subtle)] px-2 py-1 text-[10px] text-[var(--text-muted)]">{method === 'legacy_query' ? 'Legacy URL' : method === 'signature' ? 'Signature' : 'Bearer'}</span>}{log.session_id && <span className="text-xs text-[var(--text-muted)]">SID: {log.session_id}</span>}</span><span className="flex items-center gap-3 text-xs text-[var(--text-muted)]"><time dateTime={log.created_at}>{new Date(log.created_at).toLocaleString()}</time><span aria-hidden="true">{expanded ? '−' : '+'}</span></span></button>{expanded && <div id={panel} role="region" aria-labelledby={`${panel}-trigger`} className="border-t border-[var(--border-subtle)] bg-[var(--bg-surface)]/50 px-4 py-4"><p className="text-xs text-[var(--text-muted)]">Operational metadata: {log.metadata?.payload_key_count || 0} source fields, {log.metadata?.transformed_field_count || 0} mapped fields, result {log.metadata?.result_category || 'unknown'}.</p><p className="mt-2 text-xs text-[var(--text-muted)]">Privacy: payload values are intentionally not retained.</p></div>}</li>; })}</ul></div>}</Surface></>}</div>;
}
